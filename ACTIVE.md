# ACTIVE

**Focus:** UX spec **Phase 2** (D-045). **Phase 1 complete** — stops as data, per-stop framing and end cards, missions (Clock Lab ×2, River ×2), Continue card, resume chip. **2a done:** top bar is ⌂ Home · Tours · Explore (mega-menu) · Guide (with the Learn / Workbench switch); "Lab mode" is now "Workbench mode". **2b done:** Home in five blocks with a live hero light clock (Clock Lab's own code), no sidebar.

**Next:** 2c — the lab template on every lab (right-column order, inline control hints instead of the controls table, threads → "Where this leads"). Then Phase 3 (roll-out: tokens and emphasis levels, sim scaling and readout bars, missions for every lab). Stars lab (`src/stars.js`, D-046 now) fitted in between.

**State:** 36 routes pass in Learn, Lab and school edition, with no errors and **no blank lab canvases** (new check, after a render-loop name clash). Assets at `?v=51`. Uncommitted: all of Phase 1a, SOL review fixes, headline.

**SOL live spot-check (2026-09-26):** passed on the core; four fixes adopted and built — overlay offers all three starts, tour "done" needs every stop visited, Clock Lab example matches the stop, quiz labelled optional. Assets at `?v=51`.
